package com.demo;

// This file exists only so Maven runs the full build lifecycle (compile, resources, jar, shade)
// and caches ALL plugins. The built artifact is discarded — only the .m2 cache matters.
public class Placeholder {}
