apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: app-b
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-b
    release: {{ .Release.Name }}
